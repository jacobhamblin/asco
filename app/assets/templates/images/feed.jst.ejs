
<div class="grida-images">
  <div class="feed-header">
    <div class="white-pixel"
      <img src="https://s3-us-west-1.amazonaws.com/asco-jkh/layout/whitepixel.png">
    </div>
    <div class="following">
      <right>
        Following
      </right>
    </div>
  </div>
  <% images.each(function (image) { %>
    <div class="item">
      <a class="image-item-a" href="#/images/<%= image.id %>"><img class="image-item" src="<%= image.escape('url') %>"></a>
      <a href="" class="image-owner">
      <% if (image.owner().escape('username')) { %>
        <%= image.owner().escape('username') %><br>
      <% } else { %>
        <%= image.owner().escape('email') %><br>
      <% } %></a>
    </div>
  <% }); %>
</div>
